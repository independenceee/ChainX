export class Cip68Service extends MeshAdapter {
  mint = async ({
    address,
    assetName,
    quantity,
    metadata,
  }: {
    address: string;
    assetName: string;
    quantity: number;
    metadata: Record<string, string>;
  }): Promise<string> => {
    const unsignedTx = await 
  };
}
